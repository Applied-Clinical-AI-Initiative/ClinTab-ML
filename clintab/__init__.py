"""clintab
Core library: preprocessing/ML (ml.py), clinical epi calculators (epi.py),
data profiling (stats.py), restricted cubic splines (spline.py), matplotlib
rendering (plots.py), and filesystem/session helpers (store.py).

Flask (app.py / routes.py) and the CLI (clintab_cli.py) both import from
here; nothing in this package depends on Flask.
"""
